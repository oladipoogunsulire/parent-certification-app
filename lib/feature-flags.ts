// Feature flags — set to true to enable, false to disable
// Do not delete flagged features — only toggle visibility here

/** When false: exam entry points are hidden from the user-facing UI */
export const EXAMS_ENABLED = false

// Black Belt exam — set to true when exam is ready for users
export const BLACK_BELT_EXAM_ENABLED = false

/**
 * When true: users see a Track browser as the entry point
 * When false: users are auto-routed into the single track silently
 */
export const MULTI_TRACK_ENABLED = false
